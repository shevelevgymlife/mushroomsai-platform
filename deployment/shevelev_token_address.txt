# Адрес контракта токена SHEVELEV в сети Decimal Smart Chain (ERC-20).
# Укажите ОДНУ строку с адресом 0x… (без кавычек). Строки с # игнорируются.
#
# Либо задайте переменную окружения SHEVELEV_TOKEN_ADDRESS (например в Render).
# Адрес можно скопировать в MetaMask: токен → Подробности → Контракт.
#
# 0x0000000000000000000000000000000000000000
